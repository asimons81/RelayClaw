# CANONICAL SOURCE — RelayClaw v1.0.0

**This directory is the single source of truth for RelayClaw.**

## What's here

```
relayclaw/
├── index.ts              Plugin entry point (708 lines)
├── src/                  Modular source (15 modules)
│   ├── handoff/          create, inject, chain, schema, md-export
│   ├── cost/             ledger, accumulation
│   ├── heartbeat/        sender, monitor, snapshot
│   ├── queue/             dequeue, enqueue, conflict
│   └── ...
├── supabase/
│   └── migrations/       20260328000000_relayclaw_init.sql
├── openclaw.plugin.json  Plugin manifest
├── package.json          v1.0.0
├── README.md
├── CHANGELOG.md
├── CONTRIBUTING.md
└── LICENSE (MIT)
```

## What was fixed / promoted here

- before_tool_call identity injection (security: prevents agent identity forgery)
- inject gateway method (proven from extension)
- complete gateway method with cost_ledger write (proven from extension)
- cost.summary gateway method (proven from extension)
- agent_end → flushLedger wiring
- Hardcoded Telegram IDs removed
- MIT LICENSE added
- Extension directory marked LEGACY

## What still has stubs (non-blocking for repo-only launch)

- approve / reject / list / inspect gateway methods — require identity-gated source
- These are documented as `"approve not yet implemented via gateway"` in the code

## Release command

```bash
cd /home/tony/.openclaw/workspace/relayclaw
npm run typecheck   # must pass
npm test            # vitest (currently a no-op scaffold)
git init && git remote add origin https://github.com/relayclaw/relayclaw
git add .
git commit -m "RelayClaw v1.0.0 canonical — before_tool_call, inject/complete gateway, cost ledger"
git tag v1.0.0
git push origin main --tags
```
